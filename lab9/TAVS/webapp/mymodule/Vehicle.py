class Vehicle:
	"Stores every vehicle detail"
	def __init__():
		self.v_id = 0
		self.isAC = false
		self.v_name = ''
		self.miles_run = 0
		self.cost_incurred = 0
		self.price = 0
		self.fuel_consumed = 0
		self.isOwned = false
    
    def getDetails(v_id, vehicle_details):
    	if v_id = self.v_id:
    		vehicle_details.append(self.v_name)
    		vehicle_details.append(self.price)
    		vehicle_details.append(self.isAC)
    		return True
    	else:
    		return False
